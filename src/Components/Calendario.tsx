import { useState } from "react";
import { useNavigate, Link } from "react-router-dom";
import { Box, Typography } from "@mui/material";

export function Calendario() {
  return (
    <Box>
      <Typography>Disponibilità Imbarcazione</Typography>
      {/* Qui verrà creato il "modulo" per prenotare il mezzo avendo a disposizione un calendario per vedere la disponibilità */}
    </Box>
  );
}
